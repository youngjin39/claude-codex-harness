<!-- generated:start -->
# Patterns Index

> Auto-generated index of `docs/patterns/`. Do not edit between the
> `generated:start` / `generated:end` markers — run the index renderer
> instead.

## Catalogue (3 patterns)

| pattern | source | kind | adoption targets | license |
|---|---|---|---|---|
| [bounded-review-plane](bounded-review-plane.md) | curriculum-workspace-source | rollout_class | learning_low_code, template_transitional | Apache-2.0 |
| [app-product-flutter](app-product-flutter.md) | flutter-app-product-source | archetype | app_product_flutter | Apache-2.0 |
| [content-workspace](content-workspace.md) | content-workspace-source | archetype | content_workspace, hybrid_pipeline | Apache-2.0 |

## Quick selection

| If the repository is... | adopt |
|---|---|
| A Flutter / native app product with `lib/` + `test/` + `integration_test/` | `app-product-flutter` |
| A curriculum / docs / wiki workspace | `bounded-review-plane` |
| A narrative / score / prompt authoring workspace | `content-workspace` |

## Adoption checklist

1. Confirm the pattern's `adoption_targets` include the recipient's
   archetype + management_mode.
2. Open an import spec referencing the source repository.
3. Run the canary apply on a single high-fit recipient.
4. If clean, fan out. If not, the rollback path restores the recipient.

<!-- generated:end -->
